[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "B"]
ignore = ["E501", "B008"]

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q --cov=agent --cov=backend"
testpaths = [
    "tests",
]
pythonpath = "."
